# Header-only shared protocol helpers (ported utils.h): DualSense CRC helpers
# and the SetStateData output-report layout, shared by bt / state_mgr / cmd /
# audio.
idf_component_register(
    INCLUDE_DIRS "include"
)
